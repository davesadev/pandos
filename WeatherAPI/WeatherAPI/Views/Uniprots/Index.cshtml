@model IEnumerable<WeatherAPI.Models.Uniprot>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccessionNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntryStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sequence)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccessionNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sequence)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UniprotId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UniprotId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UniprotId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
